# 🎬 Киновекна 2026 — Автоматические аудио-новости кино

Полностью автоматизированный сайт аудио-новостей о кино. Собирает новости с RSS-лент, озвучивает через TTS, публикует на GitHub Pages.

## ⚙️ Как это работает

```
RSS-источники → Парсинг → Обработка Claude AI → Озвучка (edge-tts) → GitHub Pages
     ↑                                                                      ↓
  Каждые 6 часов ← ← ← ← GitHub Actions автоматизация ← ← ← ← Сайт обновлён
```

1. **Сбор новостей** — скрипт парсит RSS-ленты кино-сайтов
2. **Обработка** — Claude AI переписывает и сокращает новости для аудио-формата
3. **Озвучка** — edge-tts (бесплатно, Microsoft) генерирует MP3-файлы на русском языке
4. **Сборка сайта** — генерируется `index.html` с новым контентом и аудио-плеером
5. **Автоудаление** — новости старше 7 дней удаляются автоматически
6. **Деплой** — GitHub Actions коммитит и пушит на GitHub Pages

## 📡 Источники новостей (RSS)

| Источник | RSS URL | Язык |
|----------|---------|------|
| Кинопоиск | `https://www.kinopoisk.ru/news.rss` | 🇷🇺 |
| KinoNews.ru | `https://www.kinonews.ru/rss.php` | 🇷🇺 |
| Filmz.ru | `https://www.filmz.ru/rss/news.xml` | 🇷🇺 |
| Кино-говно.ком | `https://www.kino-govno.com/rss` | 🇷🇺 |
| Deadline Hollywood | `https://deadline.com/feed/` | 🇺🇸 |
| Variety | `https://variety.com/feed/` | 🇺🇸 |
| The Hollywood Reporter | `https://www.hollywoodreporter.com/feed/` | 🇺🇸 |
| Screen Rant | `https://screenrant.com/feed/` | 🇺🇸 |
| Collider | `https://collider.com/feed/` | 🇺🇸 |
| IndieWire | `https://www.indiewire.com/feed/` | 🇺🇸 |

## 🚀 Быстрый старт

### 1. Клонируй репозиторий
```bash
git clone https://github.com/YOUR_USERNAME/kinovekna-2026.git
cd kinovekna-2026
```

### 2. Установи зависимости
```bash
pip install -r requirements.txt
```

### 3. Настрой API-ключ (опционально, для Claude AI)
```bash
# В GitHub Secrets или .env файле:
ANTHROPIC_API_KEY=sk-ant-...
```

### 4. Запусти вручную
```bash
python scripts/fetch_news.py      # Собрать новости
python scripts/generate_audio.py  # Озвучить
python scripts/build_site.py      # Собрать сайт
```

### 5. Включи GitHub Actions
GitHub Actions автоматически запускается каждые 6 часов.
Или запусти вручную: Actions → "Update News" → Run workflow

## 📁 Структура проекта

```
kinovekna-2026/
├── .github/workflows/
│   └── update-news.yml     # Автоматизация (каждые 6ч)
├── scripts/
│   ├── fetch_news.py       # Парсинг RSS
│   ├── generate_audio.py   # TTS озвучка (edge-tts)
│   ├── build_site.py       # Сборка HTML
│   └── cleanup.py          # Автоудаление старых новостей
├── templates/
│   └── base.html           # HTML-шаблон сайта
├── data/
│   └── news.json           # База новостей (auto-generated)
├── audio/                  # MP3 файлы (auto-generated)
├── index.html              # Финальный сайт (auto-generated)
├── requirements.txt        # Python зависимости
└── config.py               # Конфигурация
```

## ⏱ Автоматизация

GitHub Actions workflow запускается:
- **Каждые 6 часов** (по крону)
- **По пушу** в main ветку
- **Вручную** через GitHub UI

## 🔧 Настройка

Отредактируй `config.py`:
- `MAX_NEWS_AGE_DAYS` — через сколько дней удалять новости (по умолчанию 7)
- `MAX_NEWS_COUNT` — макс. количество новостей на сайте (по умолчанию 20)
- `TTS_VOICE` — голос для озвучки (по умолчанию `ru-RU-DmitryNeural`)
- `RSS_FEEDS` — список RSS-источников

## 📜 Лицензия

MIT
